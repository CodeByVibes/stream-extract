name: Linux CLI

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-24.04
    env:
      SMOKE_FIXTURES: |
        ${{ github.workspace }}/dist/fixtures/sample.mp4
        ${{ github.workspace }}/dist/fixtures/sample.mkv
        ${{ github.workspace }}/dist/fixtures/sample-rich.mkv
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.x
      - name: Install packaging dependencies
        run: sudo apt-get update && sudo apt-get install --yes binutils build-essential curl squashfs-tools tar
      - name: Validate and package
        run: build/linux/fetch-native-tools.sh
      - name: Smoke test
        run: |
          build/linux/generate-fixtures.sh
          build/linux/smoke-test.sh
      - name: Upload Linux packages
        uses: actions/upload-artifact@v4
        with:
          name: streamextract-linux-x64
          path: |
            dist/streamextract-linux-x64.tar.gz
            dist/StreamExtract-x86_64.AppImage
      - name: Upload release asset
        if: github.event_name == 'release' && github.event.action == 'published'
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          files: |
            dist/streamextract-linux-x64.tar.gz
            dist/StreamExtract-x86_64.AppImage
